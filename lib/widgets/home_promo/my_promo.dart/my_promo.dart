import 'package:flutter/material.dart';

class MyPromo extends StatelessWidget {
  const MyPromo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
